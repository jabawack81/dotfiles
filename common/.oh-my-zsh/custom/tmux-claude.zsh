# Shortcuts for the tmux-claude launcher script.
# `tc`  — start a new tmux session running claude
# `tcc` — same, but continue the previous claude session

alias tc='tmux-claude.sh'
alias tcc='tmux-claude.sh --continue'
